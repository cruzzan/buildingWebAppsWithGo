Super simple program to server files from a given directory root.

To run server:

`$ go build`

`$ ./fileserver`

Then test it by opening the following url in a browser:

`http://localhost:8181/index.html`
